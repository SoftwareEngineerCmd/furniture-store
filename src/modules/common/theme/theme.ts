import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    text: {
      primary: "#000000",
      secondary: "#333333",
      disabled : "#947458"
    },
    background: {
      paper: "#F6F4F2",
      default: "#FFFFFF",
    },
    primary: {
      main: "#947458",
      light: "#F6F4F2",
    },
  },
  
});
